# YOUTHY AI 환경변수 설정 예시
# 이 파일을 .env로 복사하고 실제 값으로 변경하세요

# ========================================
# 필수 API 키
# ========================================

# 서울 열린데이터광장 API 키
SEOUL_OPEN_DATA_API_KEY=your-seoul-open-data-api-key-here

# OpenAI API 키 (ChatGPT 사용 - 권장)
OPENAI_API_KEY=your-openai-api-key-here

# ========================================
# 대안 LLM 설정 (선택사항)
# ========================================

# Hugging Face 토큰 (무료 LLM 사용시)
HF_API_TOKEN=your-huggingface-token-here

# LLM 타입 선택
LLM_TYPE=openai  # openai(권장), ollama(로컬), huggingface(무료), template(테스트)

# ========================================
# 데이터베이스 설정
# ========================================

DB_HOST=localhost
DB_PORT=5432
DB_NAME=youthy_ai
DB_USER=kimko
DB_PASSWORD=

# ========================================
# 서버 설정
# ========================================

PORT=8000
DEBUG=true

# ========================================
# 캐시 설정
# ========================================

# 로컬 캐시 사용 여부
USE_LOCAL_CACHE=true

# 캐시 만료 시간 (시간)
CACHE_EXPIRY_HOURS=24

# 자동 갱신 간격 (시간)
AUTO_REFRESH_INTERVAL=24
